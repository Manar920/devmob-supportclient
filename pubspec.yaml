name: devmob_supportclient
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.6.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

  #  Dépendances Firebase
  firebase_core: ^2.30.0
  firebase_auth: ^4.17.6
  cloud_firestore: ^4.17.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # Ajoute ici tes assets si tu en as besoin
  # assets:
  #   - images/
